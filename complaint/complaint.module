<?php
function complaint_menu(){
  $items = array();
  $items['kip/new'] = array(
    'title' => t('Test'),
    'page arguments' => array('complaint_form'),
    'file' => 'kip-form.inc',
    'page callback' => 'drupal_get_form',
    'access arguments' => array('access content'),
  );
  
  return $items;
}